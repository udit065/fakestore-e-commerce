import Particles from "react-tsparticles";
import ParticleConfig from '../components/particle-config/Particle-config';

const ParticleBg = () => {
    return (
        <Particles params={ParticleConfig}>

        </Particles>
    )
}

export default ParticleBg;